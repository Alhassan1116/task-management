version: "3.9"

services:
  app:
    build: .
    ports:
      - "${FLASK_PORT:-5000}:5000"
    env_file:
      - .env
    volumes:
      - db-data:/app/instance
    restart: unless-stopped

volumes:
  db-data:
